---
- name: Restart redis service
  ansible.builtin.systemd_service:
    name: redis.service
    daemon_reload: true
    state: restarted
  throttle: 1
  ignore_errors: '{{ ansible_check_mode }}'
...
