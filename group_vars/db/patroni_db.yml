---
patroni_db_api_endpoint: https://{{ ip_address }}:8008

patroni_db_users:
  netbox:
    password: '{{ netbox_db_password }}'

patroni_db_add:
  netbox:
    owner: netbox

patroni_db_privs:
  - login_db: netbox
    objs: public
    privs: CREATE
    roles: netbox
    type: schema
...
